# dependencies
node_modules/

# next.js
.next/
out/
api/.next/
api/out/

# production
build/
api/build/

# testing
coverage/

# env files (secrets never committed)
.env
.env.*
!.env.example
!api/.env.example

# local databases (SQLite dev/test files) — global guard, nothing tracked ever
*.db
*.db-journal
api/db/*.db
api/db/*.db-journal
api/prisma/*.db

# logs
*.log

# misc
.DS_Store
*.pem
*.tsbuildinfo
next-env.d.ts

# sandbox / editor tooling (not part of the product)
.zscripts/
download/
upload/
skills/
.claude
.z-ai-config

# sandbox ambient DATABASE_URL target
/db/*.db
/db/*.db-journal

# sandbox gateway config
Caddyfile
