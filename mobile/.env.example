# Base URL of the Dr_Booking REST API the app talks to.
# Local development (api/ runs on port 3000):
EXPO_PUBLIC_API_URL=http://localhost:3000

# Phase 9 (EAS production build) replaces this with the deployed API URL.
# Leave the value empty here — real values live in .env / .env.local (gitignored).
